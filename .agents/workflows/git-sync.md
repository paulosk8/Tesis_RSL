---
description: Sync changes with git (commit and push)
---

This workflow automates the process of staging all changes, committing them with a message, and pushing to the remote repository.

1. Prompt the user for a commit message.
2. Run the sync script:
   ```bash
   ./git-sync.sh "your commit message"
   ```
